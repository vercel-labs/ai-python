---
title: "ai.events agent events"
description: Reference for agent-level events.
type: reference
summary: Reference for ToolCallResult, PartialToolCallResult, and HookEvent.
---

Agents can emit event values that are not raw provider stream events.

## Types

- `ToolCallResult`
- `PartialToolCallResult`
- `HookEvent`

`ToolCallResult` carries the tool result message and result parts.
`PartialToolCallResult` carries values yielded by streaming tools and
`yield_from`. `HookEvent` carries an internal hook message and `HookPart`.
