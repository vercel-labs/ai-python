---
title: "ai.util.merge"
description: "Merge async iterables into one stream."
type: reference
summary: "Reference for ai.util.merge."
---

`merge` consumes multiple async iterables concurrently and yields items as
they become available.

```python
async for event in ai.util.merge(stream, tool_runner.events()):
    ...
```
