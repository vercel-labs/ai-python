---
title: "ai.mcp.get_http_tools"
description: "Load tools from an MCP HTTP server."
type: reference
summary: "Reference for ai.mcp.get_http_tools."
---

`get_http_tools` connects to an MCP HTTP server and returns `AgentTool`
values that can be passed to `ai.agent`.

```python
tools = await ai.mcp.get_http_tools("https://example.com/mcp")
```

Use `tool_prefix` when multiple servers expose overlapping tool names.
